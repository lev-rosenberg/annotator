import { createSlice } from '@reduxjs/toolkit'

//fill this out later when i need to add manage stage :)

const initialState = {
};
  
const annotateSlice = createSlice({
    name: 'annotator-tool',
    initialState,
    reducers: {},
  })

  // Extract the reducer and actions
const { reducer, actions } = annotateSlice;
export default reducer

